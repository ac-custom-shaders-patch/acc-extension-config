; Fix for linear color space messing up dashboard. Originally, it uses large negative colors with very transparent
; texture, and there is no way I can remap that to be rendered correctly, but I can just boost transparency.

[DATA]
ACTIVE = $" read('settings/linear_color_space', false) and read('csp/version', 0) >= 3834 "
DI_BASE_ALPHA = 20
